import React from "react";

const HrSection = ({ className }) => (
  <div
    className={
      `text-center my-12 text-gray-900 dark:text-navy-lighter ` + `${className}`
    }
  >
    &#9670; &#9670; &#9670;
  </div>
);

export default HrSection;
