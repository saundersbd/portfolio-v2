import React from "react";

const HrSection = ({ className }) => (
  <div
    className={
      `text-center my-12 text-stone-800 dark:text-gray-400 ` + `${className}`
    }
  >
    &#9670; &#9670; &#9670;
  </div>
);

export default HrSection;
